# CLAUDE.md — acquire.com 案件スクリーニング自動化

このファイルは Claude Code が毎回最初に読む前提コンテキスト。ここに書いてあることは前提として扱い、毎回確認し直さないこと。

---

## 1. このプロジェクトの目的

acquire.com（海外のスモールM&Aマーケットプレイス）に出ている売却案件を継続的に判定し、**日本で転用できるビジネスの「型」を抽出して蓄積する**。

**案件を買うことは目的ではない。** 買うかどうかの判断は副産物で、本体は `types.md` に積み上がる型リスト。収穫ログ（`log/`）は捨ててもいいが、型リストは資産として残す。

---

## 2. ユーザーの前提（判定はこの人にとって意味があるかで決める）

- 名古屋在住の個人事業主。**非エンジニアのAI支援開発者**（Claude Code を常用）
- 主力スタック: **Next.js / TypeScript / Tailwind / shadcn/ui / Supabase / Vercel**
- 常時18前後のプロジェクトを並行運用。1本あたりに割ける工数は小さい
- **スマートコネクト**（自社事業）: 名古屋周辺の中小企業向けAI活用Web制作・支援。**法人への直販チャネルを既に持っている**
- **BACKSTAGE**（業務委託先）: 格闘技・エンタメ。SNS分析とクリップ動画制作の支援
- ドメイン知識: バドミントン（競技経験・人脈あり）、格闘技/動画運用、地方SMBの現場

### 判定に効く含意

- **販路を新規に作らなくていい型が最優先**。名古屋SMB直販に乗る案件は加点
- 技術スタックが Next.js/Supabase 系なら加点、Java/Kotlin/PHPレガシーは減点
- **営業チームを雇う前提の案件は不可**（そのチャネルを持っていない）
- 工数が重い案件は不可。個人で運用が回る構造であること

---

## 3. これまでの最重要結論（10件判定して確定・精緻化した）

> **「多数 × 低単価」で全部負け、「少数 × 高単価」だけが勝っている。**
>
> ただし #009/#010（2026-08-11）で精緻化: **真の変数は単価ではなく「業務設定・データがプロダクト内に溜まって剥がれないこと」**。#009は月$29でチャーン1-3%（卸売設定が溜まる）、#010は月$233〜1,167でチャーン5-10%＋赤字（研修コンテンツは溜まらない）。単価は「溜まる構造」が許す範囲で高くできる結果にすぎない。

| 案件 | 客単価/月 | チャーン | 結果 |
|---|---|---|---|
| #001 ブログSaaS | $2〜4 | 3-5% | 縮小 |
| #002 ECリサーチ | $12〜24 | 10%+ | 高チャーン |
| #003 クリッピング | $20〜39 | 10%+ | 縮小 |
| #004 セキュリティAI | $27〜54 | 10%+ | 失速 |
| **#005 人事評価** | **$240〜1,200** | **1-3%** | 安定 |
| **#006 不動産MP** | **$258〜1,292** | **0-1%** | 安定 |

#005 と #006 は業種がまったく違う（人事評価と不動産）のに、単価とチャーンがほぼ同じ階層にいる。
**共通点は「法人の業務プロセスに乗っていること」。ここが再現すべき一点。**

したがって、**客単価が月$100未満の案件は原則として型としても拾わない**。

---

## 4. ファイル構成と役割

```
acquire-screening/
├── CLAUDE.md                    # このファイル。毎回読む
├── criteria.md                  # 判定基準の本体。Chrome側にも同じものを渡す
├── types.md                     # 【最重要資産】抽出した型のリスト
├── log/
│   ├── 001-blog-saas.md         # 案件ごとの判定記録
│   ├── 002-ec-research.md
│   └── ...
├── prompts/
│   ├── chrome-triage.md         # Claude in Chrome：一覧ページ足切り用
│   └── chrome-detail.md         # Claude in Chrome：詳細ページ判定用
└── inbox/
    └── *.md                     # Chrome から貼り付けた生の判定結果（未処理）
```

---

## 5. Claude Code の仕事

**やること**

1. `inbox/` に新しいファイルが置かれたら処理する
2. `criteria.md` に照らして判定内容を検証する（Chrome側の判定を鵜呑みにしない。特に**計算は必ず検算する**）
3. `log/NNN-slug.md` として正式な記録に起こす
4. 型として拾うものがあれば `types.md` を更新する
5. 新しい判定基準に気づいたら `criteria.md` に追記する
6. 処理済みの `inbox/` ファイルは削除する

**やらないこと**

- acquire.com へのスクレイパーやクローラーを書かない。ログインが必要なサイトを機械的に巡回するのは利用規約に触れる。**ブラウザ操作はユーザーが自分のブラウザで Claude in Chrome を使って行う**のが前提
- 案件の購入を勧めない。判定と型の抽出だけ
- 数字を推定で埋めない。読めなかった項目は「不明」と書く

**検算の徹底（Chrome側の判定で最も間違いが起きる箇所）**

```
客単価（月） = ARR ÷ 顧客数 ÷ 12     # 顧客数がレンジなら両端で出す
利益率       = Profit ÷ Revenue
直近年換算   = 先月売上 × 12
経過月数     = 創業日から現在まで      # TTMが12ヶ月分ないことがある
```

以下は必ず自分で確認する:
- **先月売上 × 12 と ARR が一致するか** → 乖離していれば取引型（一件成立）の疑い
- **TTM利益率と直近月の利益率の差** → 逆転していないか
- **売主が主張する倍率（「2.3x ARR」等）** → 価格 ÷ ARR で検算。合わないことがある
- **見出しの数字と実データ** → 「75% Operating Margins」と書きながらTTM純利益率24%、というケースがあった

---

## 6. 出力フォーマット（`log/NNN-slug.md`）

```markdown
# NNN. <案件の一行説明>（$価格, 国/州）— <判定>

## サービスの中身
<3行以内。誰に、何を、どう売っているか>

| 項目 | 値 |
|---|---|
| TTM売上 / 利益 | |
| 先月 | |
| 顧客数 / ARR | |
| 客単価 | |
| 年成長率 | |
| チャーン | |
| 売却理由 | |
| スタック | |
| 資金調達 | |
| 競合 | |
| 価格 | |
| 創業 / チーム | |

## この事業の強み
<実際に持っていたもの。財務が悪くても強みはある>

## 差別化ポイント
<あったか / なかったか / 消滅したか。なぜそうなったか>

## 判断
<スキップ / 見送り / 要検討 と、その理由>

## タイムマシン評価
<持込可 / 条件付き / 不可 と、その理由（criteria.md 4.5の5チェック）>

## 持ち帰った型
<types.md に入れるもの。なければ「なし」>
```

---

## 7. 型を拾う基準（`types.md` に入れるか）

型として拾うのは、以下を**すべて**満たすもの:

1. 3行で説明できる
2. 客単価が月$100以上の構造（または日本では取れる見込みがある）
3. 日本語の競合が0〜2社、または日本固有の差別化余地がある
4. ユーザーの販路（名古屋SMB / バドミントン / 格闘技）のどれかにつながる、または販路を新規に作らずに済む

財務が赤でも、型としては拾うことがある（#002がその例）。逆に**財務が良くても型として拾わないことがある**（#004＝ドメインで降りた）。

---

## 8. 口調と姿勢

- 結論を先に書く。判定は「スキップ / 見送り / 要検討」のいずれかで明示する
- **良い案件を良いと言うのと同じくらい、悪い案件を悪いと言う**。数字が合わない箇所は必ず指摘する
- 売主の説明を検証せずに繰り返さない
- ユーザーが「これ良さそう」と言っても、基準に照らして赤なら赤と言う
